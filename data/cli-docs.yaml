git:
  title: Git
  description: "Git is a distributed version control system."
  sections:
    - title: Most Used
      commands:
        - cmd: git status
          desc: Show current changes
        - cmd: git commit -m "msg"
          desc: Commit with a message
    - title: Branching
      commands:
        - cmd: git checkout -b feature
          desc: Create new branch

docker:
  title: Docker
  description: "Docker packages software into containers."
  sections:
    - title: Common
      commands:
        - cmd: docker ps
          desc: Show running containers
